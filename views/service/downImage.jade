
style.
    input {
        background-color: #fff !important;
        border-radius: 0 !important;
        border: 0;
        display: block;
        width: 100%;
        height: 34px !important;
        line-height: 34px !important;
        padding: 0 5px !important;
        margin-top: 0;
    }
.wrapper.tc
    form#searchForm
        p.f12.bc(style="width: 900px;") 基于Phantomjs实现，可自定义抓取页面宽度，高度。PhantomJS 是一个基于WebKit的服务器端JavaScript API。它全面支持web而不需浏览器支持，其快速，原生支持各种Web标准：DOM 处理、CSS 选择器、JSON、Canvas、和SVG，可以用于页面自动化、 网络监测、网页截屏、以及无界面测试等。

        div.mt50.tc.bc.tl.form-inline(style="padding-bottom: 70px;")
            div
                input.form-control(style="width:500px;", type="url", placeholder="http://...", required, name="url")
                input.form-control.ml5(style="width: 100px", type="number", required, name="width", min="100", max="10000", placeholder="width(px)")
                input.form-control.ml5(style="width: 100px", type="number", required, name="height", min="100", max="10000", placeholder="height(px)")
                button.btn.btn-info.ml5(type="submit", style="border-radius:0; width: 120px") 网页转为图片

        
script.
    var form = document.getElementById('searchForm');
    form.onsubmit = function() {
        var params = $(form).serialize();
        oc.tools.form.loadingButton(form);
        oc.ajax.get('/service/downloadImage?' + params, function(res) {
            oc.tools.form.resetButton(form);
            window.open(res);
        });

        return false;
    }
        
        